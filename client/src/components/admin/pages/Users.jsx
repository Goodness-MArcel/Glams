import React from "react";
import UsersSkeleton from "../components/UsersSkeleton";

function Users() {
  // Render skeleton sketch for the page — replace with real data component later
  return <UsersSkeleton />;
}

export default Users;